services:
  db:
    image: postgres:15-alpine
    container_name: fin_track_db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: fintackdb
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: